apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  replicas: 2  # Số lượng replicas phải được đặt ở đây
  selector:
    matchLabels:
      app: myapp-deployment  # matchLabels chỉ chứa nhãn để khớp với Pod
  template:
    metadata:
      labels:
        app: myapp-deployment # Các Pod sẽ có nhãn này
    spec:
      containers:
      - name: myapp
        image: quyetjs/lab_argocd:tag
        ports:
        - containerPort: 8080
