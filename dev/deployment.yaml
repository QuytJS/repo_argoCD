apiVersion : apps/v1
kind: Deployment
metadata:
  name: myapp-argocd
spec:
  selector:
    matchLabels:
      app: myapp-argocd
      replicas: 2
  template:
    metadata:
      labels:
        app: myapp-argocd-1
    spec:
      containers:
      - name: myapp-argocd
        image: quyetjs/lab_argocd:tag
        ports:
        - containerPort: 8080
