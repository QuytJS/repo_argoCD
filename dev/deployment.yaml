apiVersion : apps/v1
kind: Deployment
metadata:
  name: myapp-argocd-1
spec:
  selector:
    matchLabels:
      app: myapp-argocd-1
  template:
    metadata:
      labels:
        app: myapp-argocd-1
    spec:
      containers:
      - name: myapp-argocd
        image: quyetjs/lab_argocd:tag
        ports:
        - containerPort: 8080
